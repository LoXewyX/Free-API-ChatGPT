[project]
name = "free-api-chatgpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "playwright>=1.62.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
free-api-chatgpt = "free_api_chatgpt:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "a",
]
